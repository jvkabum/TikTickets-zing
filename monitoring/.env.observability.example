# =============================================================================
# OpenTelemetry SDK Configuration
# =============================================================================

# Habilitar/Desabilitar o SDK (útil para desenvolvimento local)
OTEL_SDK_DISABLED=false

# Nome do serviço (aparece nos traces e métricas)
OTEL_SERVICE_NAME=tiktickets-backend

# Endpoint do OTel Collector (gRPC)
# ⚠️ IMPORTANTE:
#   - Desenvolvimento LOCAL (npm run dev):  http://localhost:4317
#   - Docker/Produção (containers):         http://otel-collector:4317
# 
# O default no código é otel-collector:4317 (produção)
# Para dev local, descomente a linha abaixo:
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4317
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://localhost:4317
# OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://localhost:4317

# =============================================================================
# SigNoz Configuration
# =============================================================================

# Segredo JWT para autenticação interna do SigNoz
# Gere um hash forte para produção (ex: openssl rand -hex 32)
SIGNOZ_JWT_SECRET=tiktickets_signoz_secret_key_change_me_in_prod

# Credenciais ClickHouse (Opcional - defaults usados se omitido)
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=9000
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# =============================================================================
# Grafana Configuration (Legacy - Se ainda usado)
# =============================================================================

# Credenciais de admin do Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=tiktickets2024

# URL raiz do Grafana (para links em alertas)
GRAFANA_ROOT_URL=http://localhost:3001

# =============================================================================
# Sentry Configuration (Opcional)
# =============================================================================

# DSN do Sentry para error tracking
# Deixe em branco para desabilitar Sentry
SENTRY_DSN=

# =============================================================================
# Deployment Environment
# =============================================================================

# Ambiente de deployment (aparece como label nas métricas)
DEPLOYMENT_ENV=development
